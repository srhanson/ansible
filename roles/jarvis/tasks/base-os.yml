---
#- name: ensure os is up to date
#  apt:
#    update_cache: yes
#    upgrade: full
#    cache_valid_time: 300

- name: install packages
  apt: pkg={{ item }} state=present
  with_items:
    - bash-completion
    - curl
    - exiftool
    - fuse
    - glances
    - git
    - htop
    - iftop
    - iotop
    - jq
    - lynx
    - mc
    - ncdu
    - nmap
    - openssh-server
    - python
    - reptyr
    - screen
    - smartmontools
    - sudo
    - tmux
    - tree
    - vim
    - wget
    - xfsprogs
    - youtube-dl
  tags:
    - packages

- name: "Download sublimetext's .deb file"
  get_url: url={{ sublimetext_url }} dest={{sublimetext_tmp}}
  tags: ["packages","sublimetext"]

- name: Install sublimetext
  apt: deb={{sublimetext_tmp}}
  tags: ["packages","sublimetext"]

- name: create /mnt points
  file:
    dest: "/mnt/{{ item }}"
    state: directory
    owner: nobody
    group: nogroup
    mode: 0777
  with_items: "{{ mntpoints }}"

- name: mount disks
  mount:
    name: "{{ item.name }}"
    src: "{{ item.src }}"
    fstype: "{{ item.fs }}"
    # change to 'mounted' to auto mount
    state: mounted
  with_items:
    - { name: /mnt/parity1, src: /dev/disk/by-id/wwn-0x50014ee659b087aa-part1, fs: ext4}
    - { name: /mnt/disk1,   src: /dev/disk/by-id/wwn-0x50014ee6af05b0b3-part1, fs: ext4}
    - { name: /mnt/disk2,   src: /dev/disk/by-id/wwn-0x50014ee003c67bee-part1, fs: ext4}

- name: mount mergerfs array
  mount:
    name: /mnt/storage
    src: /mnt/disk*
    opts: direct_io,defaults,allow_other,minfreespace=50G,fsname=mergerfs
    fstype: fuse.mergerfs
    # change to 'mounted' to auto mount
    state: mounted
